# NSE Relay — FINAL (No-Headless)
Endpoints:
- `/` status
- `/health` IST time
- `/nse?index=NIFTY%20200&maxAge=900` NSE index snapshot

Deploy on Railway:
- New Project → Deploy from GitHub or Local Files
- Include: Dockerfile, package.json, server.js
